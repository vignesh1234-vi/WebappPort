name: SonarCloud Scan

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      # Checkout your repository code
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # Run SonarCloud Analysis
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@b4c89556e8e28cf383e12818738c5822047cb9cb
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=vignesh1234-vi_WebappPort
            -Dsonar.organization=m-vignesh
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.sources=.
            -Dsonar.exclusions=**/*.test.js,**/*.spec.js
            -Dsonar.verbose=true
